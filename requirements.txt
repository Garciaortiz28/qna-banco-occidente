# ==============================================================================
# requirements.txt -- Asistente Virtual Banco de Occidente
# Optimizado para Railway: sin paquetes dev, sin -e file://, torch CPU-only
# Versiones extraidas del entorno virtual local (2026-05-25)
# ==============================================================================

# Torch CPU-only: evita descargar ~2 GB de CUDA en Railway (sin GPU).
# Si Railway falla al buscar este wheel, ajustar a: torch==2.6.0+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# -- Nucleo API ----------------------------------------------------------------
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-dotenv==1.2.2
pydantic==2.13.4
python-multipart==0.0.28
starlette==1.0.0

# -- LangChain -----------------------------------------------------------------
langchain==0.3.30
langchain-core==0.3.86
langchain-groq==0.3.8
langchain-google-genai==2.0.10
langchain-huggingface==0.3.1
langchain-chroma==0.2.6
langchain-text-splitters==0.3.11
langchain-community==0.3.31

# -- LangGraph + PostgresSaver -------------------------------------------------
langgraph==1.0.1
langgraph-checkpoint==3.0.1
langgraph-checkpoint-postgres==3.0.5
langgraph-prebuilt==1.0.1
langgraph-sdk==0.2.15
langsmith==0.8.5

# -- Embeddings y RAG (ChromaDB + sentence-transformers) ----------------------
sentence-transformers==5.5.0
chromadb==1.5.9
torch==2.12.0+cpu
transformers==5.8.1
tokenizers==0.22.2
huggingface-hub==1.15.0
safetensors==0.7.0
onnxruntime==1.26.0

# -- Base de datos (Supabase / PostgreSQL) ------------------------------------
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1
supabase==2.9.1

# -- LLMs y APIs externos -----------------------------------------------------
groq==0.37.1
google-generativeai==0.8.6
google-ai-generativelanguage==0.6.15

# -- WhatsApp / Comunicaciones -------------------------------------------------
twilio
resend==2.30.1

# -- HTTP y async --------------------------------------------------------------
requests==2.34.2
aiohttp==3.13.5
httpx==0.27.2
tenacity==9.1.4

# -- Analisis y datos ----------------------------------------------------------
numpy==2.4.5
scikit-learn==1.8.0
scipy==1.17.1
plotly==6.7.0

# -- Voz: TTS y subida de audio -----------------------------------------------
gtts==2.5.4
cloudinary==1.44.2
pydub==0.25.1